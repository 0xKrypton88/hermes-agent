import sys; _r=next((str(__import__("pathlib").Path(m.MAPPING["hermes_cli"]).parent) for m in list(sys.modules.values()) if isinstance(getattr(m,"MAPPING",None), dict) and "hermes_cli" in m.MAPPING), ""); _r and _r not in sys.path and sys.path.insert(0,_r); import hermes_environ_startup; hermes_environ_startup.remember_process_origin()
